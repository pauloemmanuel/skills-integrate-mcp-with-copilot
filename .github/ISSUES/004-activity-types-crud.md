# CRUD for activity types and admin UI endpoints

**Description**

Add CRUD endpoints for activity types (e.g., `club`, `sport`, `class`) and allow admins to manage them. Ensure activities reference an activity type.

**Acceptance criteria**

- `GET/POST/PUT/DELETE` endpoints for activity types exist.
- Activities reference a valid activity type.

**Labels:** enhancement, backend
**Priority:** medium
